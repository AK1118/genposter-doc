# Axis

**Axis** 是一个枚举类型，用于表示布局的主轴方向。它在许多布局组件（如 Row、Column、Wrap）中被用来确定子组件的排列方向。

import { PropsTable } from "@/components/ui/props-table";

<PropsTable
  titles={{
    name: "枚举名称",
    value: "枚举值",
  }}
  data={[
    {
      name: "horizontal",
      type: "horizontal",
      default: "horizontal",
      description: "沿水平方向排列（从左到右）。常用于 Row 等组件。",
      required: true,
    },
    {
      name: "vertical",
      type: "vertical",
      default: "vertical",
      description: "沿垂直方向排列（从上到下）。常用于 Column 等组件。",
      required: true,
    },
  ]}
/>
