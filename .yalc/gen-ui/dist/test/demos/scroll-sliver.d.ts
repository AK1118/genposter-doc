import { BuildContext } from "@/lib/basic/elements";
import { StatelessWidget, Widget } from "@/lib/basic/framework";
/**
 * # 滚动虚拟列表例子
 *  -
 */
export default class ScrollSliverListExample extends StatelessWidget {
    private _controller;
    private get autoKeepAlive();
    private get itemCount();
    build(context: BuildContext): Widget;
}
